function countingBackwards() {
    let resultado= [];  
    for (let counter = 20; counter >= 1; counter--) {
       resultado.push(counter);
    }
    return resultado;  
  };
    countingBackwards();